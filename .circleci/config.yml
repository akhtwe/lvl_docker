version: 2.1
# Define the jobs we want to run for this project
jobs:
  build:
    docker:
      - image: charrtoo/lvl_docker:v1
        auth:
          username: charrtoo
          password: devDocker@AKH123  # context / project UI env-var reference
    steps:
      - run: echo "this is the build job"
# Orchestrate our job run sequence
workflows:
  build_:
    jobs:
      - build